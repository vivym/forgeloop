import { PageHeader, Section } from '../../../shared/layout';

export default function RunsRoute() {
  return (
    <>
      <PageHeader title="Runs" subtitle="Run sessions and execution outcomes will be listed here." />
      <Section title="Run list" description="This skeleton reserves the run sessions index route.">
        <p>No runs are loaded in this skeleton.</p>
      </Section>
    </>
  );
}
