import { ScaffoldRoute } from '../_scaffold';

export default function NewInitiativeRoute() {
  return (
    <ScaffoldRoute
      notice="Initiative creation will write through typed product commands."
      sectionTitle="New initiative"
      subtitle="Capture an initiative narrative and delivery context."
      title="New Initiative"
    />
  );
}
