import { ScaffoldRoute } from '../_scaffold';

export default function ObservationReportRoute() {
  return (
    <ScaffoldRoute
      notice="Observation report data is loading from project-management read models."
      sectionTitle="Observation report"
      subtitle="Post-release observations and evidence."
      title="Observation Report"
    />
  );
}
