import { ScaffoldRoute } from '../_scaffold';

export default function ReplayReportRoute() {
  return (
    <ScaffoldRoute
      notice="Replay report data is loading from project-management read models."
      sectionTitle="Replay report"
      subtitle="Delivery replay and execution history."
      title="Replay Report"
    />
  );
}
