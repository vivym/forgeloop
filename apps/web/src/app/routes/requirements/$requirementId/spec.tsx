import { ScaffoldRoute } from '../../_scaffold';

export default function RequirementSpecRoute() {
  return (
    <ScaffoldRoute
      notice="Requirement spec authoring will be connected to the typed requirement context."
      sectionTitle="Spec authoring"
      subtitle="Author or review the spec attached to this requirement."
      title="Requirement Spec"
    />
  );
}
